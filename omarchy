#!/usr/bin/env bash

set -euo pipefail

if [[ -z "${STOW_FOLDERS:-}" ]]; then
    STOW_FOLDERS="bash-omarchy,bin,git,nvim,tmux,tmux-sessionizer,herdr"
fi

if [[ -z "${DOTFILES:-}" ]]; then
    DOTFILES="$HOME/personal/dotfiles"
fi

STOW_FOLDERS="$STOW_FOLDERS" DOTFILES="$DOTFILES" "$DOTFILES/install"
